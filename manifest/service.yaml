apiVersion: v1
kind: Service
metadata:
  name: dissertation-ai
  namespace : bits-dissertation
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: dissertation-ai
  ports:
    - port: 8888
      targetPort: 8888
      nodePort: 30007
