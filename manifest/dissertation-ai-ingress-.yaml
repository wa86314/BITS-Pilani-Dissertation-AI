apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  name: dissertation-ai
  namespace: bits-dissertation
spec:
  ingressClassName: nginx
  rules:
  - host: myapp.48.194.48.34.nip.io
    http:
      paths:
      - backend:
          service:
            name: dissertation-ai
            port:
              number: 80
        path: /
        pathType: Prefix
